{
  "postCreateCommand": "bash ${localWorkspaceFolder}/.devcontainer/setup.sh && bash ${localWorkspaceFolder}/install.sh",
  "customizations": {
    "vscode": {
      "settings": {
        "locale": "en",
        "terminal.integrated.defaultProfile.linux": "zsh"
      }
    }
  }
}
